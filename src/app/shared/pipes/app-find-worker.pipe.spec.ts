import { AppFindWorkerPipe } from './app-find-worker.pipe';

describe('AppFindWorkerPipe', () => {
  it('create an instance', () => {
    const pipe = new AppFindWorkerPipe();
    expect(pipe).toBeTruthy();
  });
});
